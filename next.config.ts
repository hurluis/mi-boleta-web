// ⚠️  Este archivo está inactivo.
// Next.js 16.x tiene un bug en su compilador SWC que produce "nextConf is not defined"
// al transpilar next.config.ts cuando la variable se llama "nextConfig".
// La config real está en next.config.js (que tiene prioridad en el lookup de CONFIG_FILES).
// No borres next.config.js ni muevas la config aquí.
